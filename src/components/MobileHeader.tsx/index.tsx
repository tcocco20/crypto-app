import HeaderActions from "./HeaderActions";
import HeaderInfo from "./HeaderInfo";

const MobileHeader = () => {
  return (
    <header>
      <HeaderActions />
      <HeaderInfo />
    </header>
  );
};

export default MobileHeader;
