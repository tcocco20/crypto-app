const MobileNavbar = () => {
  return (
    <nav className="z-50 fixed bottom-0 left-0 border-t border-t-gray-600 w-full bg-gray-800">
      MobileNavbar
    </nav>
  );
};

export default MobileNavbar;
